<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yong.demo.dao.UserDao">
    <select id="selectAllUsers" resultType="com.yong.demo.pojo.User">
        select id,username,password from user
    </select>
    <select id="selectOneById" resultType="com.yong.demo.pojo.User" parameterType="java.lang.Integer">
        select id,username,password from user where id=#{id}
    </select>
    <delete id="deleteUserById" parameterType="java.lang.Integer">
        delete from user where id=#{id}
    </delete>

    <insert id="insertUser" parameterType="com.yong.demo.pojo.User">
        insert into user (username,password)value (#{username},#{password})
    </insert>
    <update id="updateUser" parameterType="com.yong.demo.pojo.User" >
        update user set username=#{username},password=#{password} where id=#{id}
    </update>
</mapper>